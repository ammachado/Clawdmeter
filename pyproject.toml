[project]
name = "clawdmeter"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = [
    "bleak>=0.22",
    "httpx>=0.27",
]

[dependency-groups]
windows = ["pystray", "pillow"]

[tool.uv]
package = false
